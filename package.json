{
  "name": "tp-fullstack-mist-root",
  "version": "1.0.0",
  "description": "Root package.json for managing dev tools like Husky",
  "scripts": {
    "prepare": "husky",
    "test": "cd user-service && npx jest --verbose && cd ../library-service && npx jest --verbose",
    "test:user": "cd user-service && npx jest --verbose",
    "test:library": "cd library-service && npx jest --verbose"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}
